<Page
    x:Class="Converter.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>       
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="0" Grid.Row="0" FontSize="18" Text="{x:Bind ViewModel.LeftMoney, Mode=TwoWay}" />
        <TextBox Grid.Column="2" Grid.Row="0" FontSize="18" Text="{x:Bind ViewModel.RightMoney, Mode=TwoWay}"/>

        <TextBlock Grid.Column="0" Grid.Row="1" FontSize="18" Text="{x:Bind ViewModel.LeftExchange.Name, Mode=TwoWay}"/>
        <TextBlock Grid.Column="2" Grid.Row="1" FontSize="18" Text="{x:Bind ViewModel.RightExchange.Name, Mode=TwoWay}"/>

        <HyperlinkButton Grid.Column="0" Grid.Row="2" Content="Click to go to page 2"
                 Click="HyperlinkButton_Click"
                 HorizontalAlignment="Center"/>
        <HyperlinkButton Grid.Column="2" Grid.Row="2" Content="Click to go to page 2"
                 Click="HyperlinkButton_Click"
                 HorizontalAlignment="Center"/>
        
        
    </Grid>
</Page>
